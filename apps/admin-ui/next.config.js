/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export' removed since we are using Next.js API Routes which require Node.js Serverless Functions on Vercel
};

module.exports = nextConfig;
